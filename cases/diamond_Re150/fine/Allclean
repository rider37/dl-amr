#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"
shopt -s nullglob
items=(
    [1-9]*
    0.[0-9]*
    postProcessing
    processor*
    constant/polyMesh
    log.*
)
if [ ${#items[@]} -gt 0 ]; then
    rm -rf "${items[@]}"
fi
