#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
rm -rf processor* postProcessing log.* *.OpenFOAM *.foam
find . -maxdepth 1 -type d -regex '\./[0-9][0-9.]*' ! -name '142.5' -exec rm -rf {} +
