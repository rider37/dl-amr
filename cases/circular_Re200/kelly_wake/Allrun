#!/usr/bin/env bash
# circular_Re200 / kelly_wake  --  restart run, 95 -> 245
set -euo pipefail
cd "$(dirname "$0")"

if [ ! -d "95" ]; then
    echo "ERROR: restart state '95/' not found. Adaptive variants restart from a common coarse-mesh state; see ../README.md ('Restart protocol')."
    exit 1
fi
pimpleFoam > log.pimpleFoam 2>&1
