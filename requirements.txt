# Pip alternative to env.yml. Install: pip install -r requirements.txt
# For GPU PyTorch, install separately following https://pytorch.org/get-started/locally/

numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
pyyaml>=6.0
tqdm
rich

# CFD / mesh I/O
pyvista>=0.43
vtk>=9.2

# Deep learning (CPU/GPU; install torch matching your CUDA from pytorch.org)
torch>=2.1
torchvision

# Optional figure conversion
img2pdf>=0.5
